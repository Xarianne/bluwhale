# System-wide just recipes for post-install setup
# Run from anywhere with: just --justfile /usr/share/ublue-os/just/Justfile <recipe>

# Set up virtualization with virt-manager (Flatpak) and USB passthrough
[group('Post-Install Setup')]
setup-virtualization:
    just --justfile /usr/share/ublue-os/just/20-virtualization.just setup

# Check virtualization setup
[group('Post-Install Setup')]
check-virtualization:
    just --justfile /usr/share/ublue-os/just/20-virtualization.just check

# Remove virtualization setup
[group('Post-Install Setup')]
remove-virtualization:
    just --justfile /usr/share/ublue-os/just/20-virtualization.just remove
