---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      # Dependencies to update hyprland
      packages:
        - meson
        - cmake
        - g++
        - gcc
        - openssl
        - git